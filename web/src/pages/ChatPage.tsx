/**
 * Stub para /chat — o conteúdo real é servido como plugin pelo backend.
 * Este arquivo existe apenas para que a rota seja reconhecida.
 * O plugin-manifest sobrescreve este componente via `tab.override`.
 */
export default function ChatPage() {
  return <div>Chat Plugin Loading...</div>;
}
